# environment_variables — exported shell environment settings

export TERM=xterm-256color
export EDITOR=nvim

# Central flake path — used by NixOS rebuild aliases and dev shells
export NIXLAB="$HOME/nixlab"
